# Level 01 — Orientation

You are in a terminal, and here is what you will be assigned to do.

## Your task

1. Find out which directory you are by typing `pwd` command.
2. Find out who you are by typing `whoami` command
3. Create a .txt file and paste your outputs of the 2 previous commands within the .txt file.
4. Verify that you did as expected by running the bash script (submission instructions below) 



## Commands

| Command | What it does |
|---------|--------------|
| `pwd`   | print working directory — where am I 	 |
| `whoami`| prints out your username (should be default) |
| `nano [filename][fileType]`| Create and enter inside the file (to esc out of file, press `ctrl + X`, to save, press `Y` to not save press `N` then type any key|
| `cat f` | print the contents of file `f` 		 |


## Submit

```bash
bash check.sh 1 
```

